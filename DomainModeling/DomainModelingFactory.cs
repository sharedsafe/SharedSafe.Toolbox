namespace DomainModeling
{
	/*
		Right now we use Konstruktor to build the default implementations for the interfaces.

		This class exists to locate the DomainModeling Assembly.
	*/

	public static class DomainModelingFactory
	{
	}
}
